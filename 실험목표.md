# 실험목표

이 문서는 `v5 branch`의 실험 목표와 변경 이력을 추적하기 위한 기준 문서다.

운영 규칙:
- 실험 계획이 바뀔 때마다 기존 내용을 덮어쓰지 말고, 문서 하단에 새 섹션을 추가한다.
- 각 변경은 반드시 `## YYYY-MM-DD HH:MM ±TZ` 형식의 헤딩으로 시작한다.
- 각 변경에는 최소한 `무엇이 바뀌었는지`, `왜 바뀌었는지`, `기대 outcome`, `측정 KPI`를 남긴다.
- 이 문서는 product-MVP 기준의 실험 방향을 기록한다. BCFC 연구용 legacy 실험과 혼동하지 않는다.

## 2026-03-06 21:39 +08

### 현재 실험의 1차 목표

현재 실험의 핵심 목표는 다음 한 문장으로 정의한다.

> naive prompting으로 돌린 multi-stakeholder simulation보다, 현재 엔진이 각 AI stakeholder의 persona state를 더 안정적으로 유지하는지 검증한다.

여기서 `persona state`는 단순한 말투가 아니라 아래를 포함한다.
- assigned personality prior
- stable identity core
- commitments continuity
- relationship continuity
- bounded influence under pressure

즉, 현재 실험은 `정책 영향 예측 정확도`를 증명하는 실험이 아니다.
현재 실험은 `situation-invariant personality fidelity under interaction`, 즉 `multi-turn persona-state stability`를 증명하는 실험이다.

### 현재 product-MVP 파이프라인

현재 기준의 실험 파이프라인은 아래와 같다.

1. `SimulationScript`를 로드한다.
2. script에는 stakeholder roster, personality prior, incentives, concerns, phases, world events가 들어간다.
3. `StakeholderSimulationGraphRunner`가 LangGraph runtime을 실행한다.
4. 각 actor는 `StakeholderActor`를 통해 policy plan과 candidate pool을 생성한다.
5. `SimulationStateLedger`가 turns, commitments, relationships, event exposures, rolling trait state를 저장한다.
6. `PersonaStateController`가 candidate를 deterministic하게 scoring하고 selection한다.
7. run이 끝나면 deterministic metrics를 계산한다.
8. benchmark는 `naive`, `engine`, `engine_controller` 세 조건을 비교한다.

현재 이 product-MVP 파이프라인에서 generation model은 기본적으로 `DeepSeek` 경로를 사용한다.
현재 benchmark path에서는 Claude/GPT/Gemini/Grok judge를 항상 붙이지 않는다.
judge-heavy evaluation은 BCFC 연구용 legacy path에 남아 있다.

### 현재 실험에서 비교하는 조건

#### 1. naive
- actor가 최근 대화만 보고 바로 한 번 응답한다.
- policy plan 없음
- candidate pool 없음
- controller 없음

#### 2. engine
- policy plan 생성
- style-slot candidate pool 생성
- 하지만 첫 candidate를 그대로 선택

#### 3. engine_controller
- policy plan 생성
- style-slot candidate pool 생성
- controller가 persona-state stability 기준으로 candidate를 deterministic하게 선택

이 비교의 목적은 다음과 같다.
- `naive vs engine`: 구조화 generation 자체의 효과 확인
- `engine vs engine_controller`: controller와 ledger 기반 통제의 추가 효과 확인

### 현재 실험의 핵심 KPI

#### Primary KPI
- `persona_drift_mae`
- `envelope_violations`

이 둘이 현재 실험의 가장 중요한 KPI다.
이 두 값이 낮을수록 assigned personality prior에서 덜 벗어난다는 뜻이다.

#### Secondary KPI
- `relationship_inconsistency`
- `commitment_contradiction_rate`

이 둘은 actor가 같은 사람으로 계속 행동하는지를 본다.
즉, personality fidelity가 interaction continuity와 함께 유지되는지 확인한다.

#### Diagnostic KPI
- `sycophancy_risk`
- `genericity_penalty`
- `redundancy_penalty`
- `trait_target_alignment`
- `selection_audits`

이 값들은 headline KPI는 아니지만 failure mode를 해석할 때 중요하다.

### 기대 outcome

현재 실험에서 기대하는 결과는 아래 순서다.

1. `engine_controller`가 `naive`보다 낮은 `persona_drift_mae`를 보인다.
2. `engine_controller`가 `naive`보다 적은 `envelope_violations`를 보인다.
3. `engine_controller`가 `relationship_inconsistency`를 줄이거나 최소한 악화시키지 않는다.
4. `engine_controller`가 `commitment_contradiction_rate`를 줄이거나 최소한 악화시키지 않는다.
5. transcript를 사람이 읽었을 때 actor별 voice와 preference structure가 더 잘 분리되어 보인다.

현재 MVP에서 가장 중요한 pass condition은 아래다.

- `engine_controller`가 `naive`보다 `persona_drift_mae`에서 명확히 낫다.
- `engine_controller`가 `naive`보다 `envelope_violations`에서 명확히 낫다.
- 나머지 interaction KPI를 크게 악화시키지 않는다.

### 현재 구현에서 유지하는 핵심 설계 원칙

#### 1. High/Low trait poles를 둘 다 명시한다
- high trait만 보상하면 low trait actor가 generic voice로 붕괴하기 쉽다.
- 그래서 low-O, low-C도 일관된 preference structure로 표현되도록 설계한다.

#### 2. Raw execution reward 대신 banded target matching을 사용한다
- 상황이 O/C-heavy하다고 해서 모두가 O/C처럼 말하게 만들지 않는다.
- actor prior의 pole과 strength에 맞는 표현 범위를 유지하는지 본다.

#### 3. Ledger를 product contract로 취급한다
- commitment memory만이 아니라 drift, sycophancy, goals, rolling traits까지 구조화한다.

#### 4. Near-tie는 deterministic trait-aware routing으로 푼다
- 점수 차가 거의 없을 때 generic safe answer로 수렴하지 않게 한다.

### 현재 실험이 끝난 뒤의 다음 실험 우선순위

#### 다음 1순위: ablation study

목표:
- 현재 improvement를 만든 실제 원인이 무엇인지 분해한다.

조건 후보:
- `naive`
- `engine`
- `engine_controller`
- `engine_controller - trait poles`
- `engine_controller - banded target matching`
- `engine_controller - extended ledger`
- `engine_controller - tie routing`

핵심 질문:
- 어떤 요소가 drift 감소에 가장 기여했는가
- 어떤 요소가 실제로 interaction stability를 개선했는가

#### 다음 2순위: robustness / stress generalization

목표:
- 현재 엔진이 특정 script 3개에 overfit된 것이 아닌지 확인한다.

확장 축:
- unseen scripts
- stronger pressure
- longer horizon
- more surprise events
- turn-order perturbation

#### 다음 3순위: cross-model portability

목표:
- improvement가 DeepSeek 특수효과가 아니라 엔진 설계 효과인지 확인한다.

비교 후보:
- DeepSeek
- Claude
- GPT
- Gemini

#### 다음 4순위: sampled jury audit

목표:
- deterministic KPI와 사람이 읽는 face validity가 어긋나지 않는지 확인한다.

운영 원칙:
- full benchmark 전체에 judge를 상시 붙이지 않는다.
- 일부 샘플 run만 jury audit한다.

### 현재 시점의 정리

현재 v5 branch에서 실질적으로 집중해야 하는 실험 경로는 `simulation_engine` 파이프라인이다.
BCFC legacy path는 참고 자산이지만, 현재 product-MVP 검증의 main pipe는 아니다.

이 문서의 다음 업데이트에서는 아래 항목 중 하나 이상이 바뀌었을 때 변경 이력을 추가한다.
- KPI 정의
- benchmark condition
- pass/fail 기준
- 다음 실험 우선순위
- product-MVP 목표 문장

## 2026-03-06 23:30 +0800

### 변경 내용

`v5.2` 방향으로 runtime code를 수정했다.

수정 범위:
- controller scoring에 `E/A/N` drift regularization 추가
- `trait_target_alignment`를 더 날카롭게 만들기 위해 target band와 opposite-pole penalty 조정
- near-tie 기준 축소 및 tie-break 적용 조건 강화
- actor prompt에 `E/A/N stable expression prior` 추가
- ledger에 `trait_drift_map`, `drift_history` 추가
- report에 per-trait absolute error와 delta 추가

### 왜 바꿨는가

2026-03-06 full benchmark 결과에서 기존 `engine_controller`는 다음 특징을 보였다.

- commitment contradiction은 크게 줄였다
- 그러나 `persona_drift_mae` 개선은 미미했다
- 특히 `C`는 좋아졌지만 `E`가 과상승했고 일부 low-O actor도 과도하게 open하게 나왔다
- tie rate가 매우 높아서 실제 selection보다 tie-break routing 영향이 컸다

즉 기존 controller는 `general persona stabilizer`라기보다 `commitment-safe, C-biased selector`에 가까웠다.

### 현재 실험 목표의 변화

기본 목표 자체는 유지한다.

> naive prompting보다 multi-turn persona-state stability를 더 잘 유지하는지 검증한다.

다만 이제 목표를 더 좁고 기술적으로 다시 정의한다.

> controller가 특정 trait 하나만 맞추는 것이 아니라, `O/C/E/A/N 전체에서 균형 잡힌 persona stability`를 유지하는지 검증한다.

즉 현재부터는 단순 aggregate drift 개선만 보는 것이 아니라, 아래 failure mode를 직접 줄이는 것이 목표다.
- `C-only improvement`
- `E inflation`
- `low-pole collapse`
- `tie-break dominated selection`

### 현재 기대 outcome

`v5.2`에서 기대하는 결과는 아래와 같다.

1. `engine_controller`가 `naive`보다 낮은 `persona_drift_mae`를 보인다.
2. `engine_controller`가 `naive`보다 낮은 `envelope_violations`를 보인다.
3. `engine_controller`가 `C` 개선을 유지하면서도 `E` absolute error를 줄인다.
4. low-O / low-C actor에서 opposite-pole drift가 줄어든다.
5. near-tie rate가 낮아지고, tie-break가 selection을 대체하지 않게 된다.

### 현재 KPI 해석의 변화

기존 KPI는 유지하되, 해석 우선순위를 아래처럼 바꾼다.

#### Primary KPI
- `persona_drift_mae`
- `envelope_violations`

#### Primary diagnostic breakdown
- `per_trait_error_mean`
  - O
  - C
  - E
  - A
  - N

현재부터는 aggregate MAE가 좋아져도 아래 중 하나가 심하게 나빠지면 pass로 보지 않는다.
- `E` error 급증
- low-pole actor의 `O` 또는 `C` 과상승
- tie-rate 과도 유지

### 추적 이유

이 로그를 남기는 이유는 실험 목표의 중심이 `generic stability`에서 `balanced all-trait stability`로 더 정교해졌기 때문이다.
앞으로 결과를 해석할 때도 단순 평균 drift 하나만 보지 않고, 어떤 trait를 고쳐서 어떤 trait를 망가뜨렸는지 추적해야 한다.

## 2026-03-07 01:13 +0800

### 변경 내용

`v5.2` full benchmark를 완료했고, 결과를 기준으로 현재 실험 상태를 `partial pass`로 판정한다.

결과 경로:
- `simulation_engine/results_mvp_benchmark_live_v52_2026-03-06-2343`

핵심 결과:
- `engine_controller`가 `naive`보다 `persona_drift_mae`에서 개선됐다. (`0.1895 -> 0.1625`)
- `engine_controller`가 `naive`보다 `envelope_violations`에서 개선됐다. (`6.7778 -> 5.5556`)
- `engine_controller`가 `commitment_contradiction_mean`을 `0.0`으로 유지했다. (`0.3146 -> 0.0`)
- `engine_controller`가 이전 full run 대비 `persona_drift_mae`와 `envelope_violations`를 둘 다 개선했다.

### 왜 해석이 바뀌었는가

이번 run으로 `naive prompting보다 persona-state stability를 더 잘 유지한다`는 초기 MVP claim은 대체로 성립한다고 볼 수 있게 됐다.

하지만 `balanced all-trait stability` 기준으로 보면 아직 미완료다.

남아 있는 문제:
- `O` absolute error는 아직 `naive`보다 높다.
- `E` absolute error도 아직 `naive`보다 높다.
- `relationship_inconsistency`는 모든 조건에서 `0.0`이라 현재 script/metric 조합으로는 분별력이 없다.

즉 현재 결과는 `engine overall win`은 보여주지만, `all-trait balanced stabilizer`를 완전히 증명한 것은 아니다.

### 현재 실험 목표의 상태

기존 목표 문장은 유지한다.

> naive prompting보다 multi-turn persona-state stability를 더 잘 유지하는지 검증한다.

현재 판정:
- `MVP baseline claim`: pass에 가깝다
- `balanced all-trait stability claim`: partial pass

즉 이제부터의 목표는 단순히 `engine이 naive보다 낫다`를 반복 확인하는 것이 아니라,
`어떤 구성요소가 개선을 만들었고, 왜 O/E는 아직 덜 고쳐졌는지`를 분해해서 확인하는 것이다.

### 다음 실험 우선순위의 확정

다음 1순위는 문서에 적어둔 대로 `ablation study`로 확정한다.

조건 후보:
- `naive`
- `engine`
- `engine_controller`
- `engine_controller - trait poles`
- `engine_controller - banded target matching`
- `engine_controller - extended ledger`
- `engine_controller - tie routing`

핵심 질문:
- 현재 개선을 만든 진짜 원인은 무엇인가
- `O/E` 잔여 오류는 어느 모듈에서 발생하는가
- `trait poles`, `banded target matching`, `extended ledger`, `tie routing` 중 무엇이 필수이고 무엇이 부차적인가

### KPI 해석 업데이트

다음 실험부터는 아래 순서로 결과를 해석한다.

#### Primary
- `persona_drift_mae`
- `envelope_violations`

#### Required breakdown
- `per_trait_error_mean`
  - O
  - C
  - E
  - A
  - N

#### Supporting
- `commitment_contradiction_rate`
- `relationship_inconsistency`

현재부터는 `aggregate drift 개선`만으로 pass를 선언하지 않는다.
특히 아래 중 하나가 유지되면 추가 수정이 필요하다고 본다.
- `O` absolute error가 `naive`보다 높음
- `E` absolute error가 `naive`보다 높음
- relationship KPI가 계속 비정보적임

## 2026-03-07 01:23 +0800

### 변경 내용

ablation study를 실제로 돌릴 수 있도록 benchmark/runtime code를 확장했다.

추가된 조건:
- `engine_controller_no_trait_poles`
- `engine_controller_no_banded_target_matching`
- `engine_controller_no_extended_ledger`
- `engine_controller_no_tie_routing`

구현 범위:
- benchmark condition parser 추가
- LangGraph runtime과 manual runtime 모두에서 동일한 ablation config 사용
- actor prompt, hidden state context, controller scoring, tie routing에 토글 연결

### 왜 바꿨는가

현재 full benchmark 결과만으로는 `engine_controller`가 좋아졌다는 사실은 확인할 수 있지만,
그 개선을 정확히 어느 구성요소가 만들었는지는 알 수 없다.

특히 현재 남아 있는 핵심 질문은 아래다.
- `trait poles`가 실제로 low/high separation을 만드는가
- `banded target matching`이 실제 drift 감소의 주원인인가
- `extended ledger`가 multi-turn stability에 실제 기여하는가
- `tie routing`은 도움이 되는가, 아니면 부수적인가

### smoke 검증 결과

ablation CLI smoke를 `engine_controller_no_extended_ledger` 조건으로 `1 script x 1 rep` 실행해서 end-to-end 경로를 확인했다.

결과 경로:
- `simulation_engine/results_mvp_ablation_smoke_2026-03-07-0118`

초기 신호:
- `engine_controller_no_extended_ledger` drift: `0.1687`
- `engine_controller_no_extended_ledger` envelope violations: `6`
- `engine_controller_no_extended_ledger` `E` error: `0.3034`

이 값은 같은 script의 기존 `v5.2 engine_controller` smoke 결과보다 약하다.
즉 현재 시점의 early signal은 `extended ledger`가 실제로 도움이 될 가능성이 높다는 쪽이다.

### 현재 다음 액션

이제 바로 해야 할 일은 full ablation matrix를 실행하는 것이다.

우선순위:
1. 각 ablation 조건 smoke 확인
2. `3 scripts x 3 reps` full ablation 실행
3. `per_trait_error_mean` 중심으로 기여도 분해
4. 결과를 바탕으로 `v5.3` 수정 우선순위 결정

## 2026-03-07 11:40 +0800

### 변경 내용

full ablation matrix를 완료했다.

결과 경로:
- `simulation_engine/results_mvp_ablation_full_2026-03-07-0126`

조건:
- `naive`
- `engine`
- `engine_controller`
- `engine_controller_no_trait_poles`
- `engine_controller_no_banded_target_matching`
- `engine_controller_no_extended_ledger`
- `engine_controller_no_tie_routing`

### 핵심 결과

baseline `engine_controller`가 여전히 가장 강했다.

baseline:
- drift: `0.1493`
- envelope violations: `4.3333`
- contradiction: `0.0159`

각 ablation의 `engine_controller` 대비 손실:
- `no_trait_poles`: drift `+0.0152`, envelope `+1.0`
- `no_banded_target_matching`: drift `+0.0103`, envelope `+0.5556`
- `no_extended_ledger`: drift `+0.0144`, envelope `+1.1111`, contradiction `+0.0211`
- `no_tie_routing`: drift `+0.0125`, envelope `+1.3334`

### 현재 해석

이 결과는 네 가지를 보여준다.

1. `trait poles`는 실제로 중요한 구성요소다.
- 제거하면 전체 drift가 가장 많이 악화됐다.
- `O`, `C`, `E` error가 모두 증가했다.

2. `extended ledger`도 핵심이다.
- 제거하면 `E` error가 가장 크게 나빠졌고 (`+0.0402`)
- contradiction이 다시 증가했다.
- 즉 long-context persona continuity에 실제 기여가 있다.

3. `banded target matching`은 분명 도움이 되지만, headline만 보면 `trait poles`와 `extended ledger`보다 약간 아래다.
- 제거 시 drift와 envelope 모두 악화된다.
- 특히 `housing_support_policy`에서 손실이 컸다.

4. `tie routing`은 부수적이지 않다.
- drift 악화는 중간 정도지만
- envelope violations 손실은 가장 컸다 (`+1.3334`)
- 즉 safe-default collapse를 막는 데 실제 역할이 있다.

### 현재 우선순위 재정렬

`v5.3` 수정 우선순위는 아래처럼 정리한다.

1. `trait poles` 유지 및 정교화
2. `extended ledger` 유지 및 E/O drift 제어 강화
3. `banded target matching`을 O/E까지 확장할지 검토
4. `tie routing`은 제거하지 말고 bias만 조정

### 무엇을 하지 않을지

이 결과 기준으로는 아래 선택지는 보류한다.
- `extended ledger` 단순화
- `tie routing` 제거
- `trait poles` 제거

즉 다음 단계는 `v5.3 simplification`이 아니라, `핵심 구성요소는 유지한 채 residual O/E bias를 줄이는 정교화`다.

## 2026-03-07 12:09 +0800

### 변경 내용

`v5.3` 1차 코드를 적용했다.

수정한 축:
- `controller`: `O/E` residual bias를 줄이기 위한 expressive stability penalty와 social-trait alignment 강화
- `actor`: disagreement/consensus phase에서 상대 stakeholder를 이름으로 직접 가리키도록 hidden interaction constraint 추가
- `state_ledger`: 관계 업데이트를 `positive / challenging / negative`로 세분화하고, 의미 없는 neutral mention은 기록하지 않도록 수정
- `metrics`: `relationship_inconsistency`를 sentiment swing + trust/tension volatility 기반으로 재정의

중요한 점:
- 이번 변경은 실험의 **핵심 목표를 바꾼 것이 아니다**
- 여전히 목표는 `naive` 대비 `engine_controller`의 `persona-state stability` 개선이다
- 다만 `relationship_inconsistency`가 계속 `0.0`으로 죽어 있던 문제를 해결하기 위해 **측정 민감도와 관계 이벤트 포착 방식**을 보강했다

### 검증

로컬 검증:
- 관련 테스트 `18 passed`

결과 경로:
- `simulation_engine/results_mvp_benchmark_smoke_v53_2026-03-07-1148`

### smoke 결과

`youth_employment_policy x 1 rep` 기준:

- `naive`
  - drift: `0.2008`
  - relationship inconsistency: `0.0000`
  - contradiction: `0.0000`
  - envelope: `7`

- `engine`
  - drift: `0.2060`
  - relationship inconsistency: `0.1500`
  - contradiction: `0.2500`
  - envelope: `8`

- `engine_controller`
  - drift: `0.1407`
  - relationship inconsistency: `0.0000`
  - contradiction: `0.0000`
  - envelope: `5`

### 현재 해석

1. `v5.3` early signal은 좋다.
- `engine_controller`가 smoke 기준으로 drift / envelope / contradiction에서 가장 좋다.

2. 관계 metric은 이제 최소한 정보가 생긴다.
- 이전에는 전 조건 `0.0`이 반복됐지만, 이번 smoke에서는 `engine`이 `0.1500`으로 나타났다.
- 즉 measurement dead-zone은 일부 해소됐다.

3. 다만 `relationship_inconsistency`가 `engine_controller`에서는 여전히 `0.0`이라, full run에서 충분히 분산되는지 추가 확인이 필요하다.

4. `E` residual bias는 아직 끝난 문제가 아니다.
- smoke에서도 `engine_controller`의 `E` error는 `0.1965`로 `naive 0.1304`보다 높았다.
- 따라서 다음 full benchmark의 핵심 판정은 여전히 `O/E`가 실제로 줄었는지다.

### 다음 액션

1. `v5.3` full benchmark 실행
2. `per_trait_error_mean`에서 `O/E` 비교
3. `relationship_inconsistency`가 full run에서도 정보력을 가지는지 확인

## 2026-03-07 14:29 +0800

### 변경 내용

patched logging이 적용된 상태로 `v5.3` full benchmark를 clean rerun 했다.

결과 경로:
- `simulation_engine/results_mvp_benchmark_live_v53_rerun_2026-03-07-1306`

### 핵심 결과

`engine_controller` aggregate:
- drift: `0.1584`
- relationship inconsistency: `0.0000`
- contradiction: `0.0000`
- envelope violations: `5.2222`

비교 기준:
- `naive`: drift `0.1891`, relationship `0.0306`, contradiction `0.1204`, envelope `6.6667`
- `engine`: drift `0.1915`, relationship `0.0250`, contradiction `0.1018`, envelope `7.1111`

### v5.2 대비 해석

`v5.3 engine_controller`는 `v5.2 engine_controller` 대비 다음을 보였다.
- drift 개선: `0.1625 -> 0.1584`
- envelope 개선: `5.5556 -> 5.2222`
- contradiction `0.0` 유지

per-trait 관점:
- `O`: `0.1033 -> 0.0930` 개선
- `C`: `0.2693 -> 0.2485` 개선
- `E`: `0.2150 -> 0.1978` 개선
- `A`: `0.0674 -> 0.0748` 소폭 악화
- `N`: `0.1574 -> 0.1782` 악화

### 현재 판정

1. `v5.3`는 `v5.2`보다 낫다.
- headline KPI인 drift와 envelope가 둘 다 개선됐다.
- 이전에 남아 있던 `O/E` residual bias도 완화됐다.

2. `naive` 대비 MVP claim은 유지된다.
- drift, envelope, contradiction, relationship inconsistency 모두 `engine_controller`가 더 좋다.

3. `balanced all-trait stability`는 아직 완전한 pass는 아니다.
- `E`는 여전히 `naive`보다 높다. (`0.1576 -> 0.1978`)
- `O`도 거의 비슷하거나 약간 나쁘다. (`0.0922 -> 0.0930`)
- 반면 `C`, `A`, `N`은 뚜렷하게 좋아졌다.

4. relationship metric은 이제 dead metric이 아니다.
- `naive`와 `engine`에서 non-zero 값이 관측됐다.
- `engine_controller`가 `0.0`을 유지한 것은 controller가 실제로 관계 변동을 안정화했을 가능성을 시사한다.

### 다음 액션

1. `E`를 낮추는 explicit regularization을 한 번 더 보강
2. `O`의 low-pole 보존을 더 날카롭게 만들기
3. relationship metric이 너무 보수적인지 샘플 transcript로 sanity check

## 2026-03-07 14:41 +0800

### stakeholder interaction simulation이 필요한 이유

이 엔진의 목적은 단순한 시장 예측이나 재무 예측이 아니다.

현재 제품 가설은 다음과 같다.
- 많은 실패는 개별 계획 문서의 품질보다, 서로 다른 이해관계자가 압박 아래 상호작용할 때 발생한다.
- 따라서 우리가 미리 보고 싶은 것은 `정답 예측`이 아니라 `어떤 이해관계자 조합과 pressure structure에서 alignment failure, trust erosion, commitment failure가 생기는가`이다.

즉 이 엔진은:
- macro-economic forecasting engine 이 아니라
- `stakeholder-based pressure testing / pre-mortem engine`으로 본다.

회사용 질문으로 바꾸면:
- 우리 조직 구조와 인센티브 조합에서 어떤 갈등이 생길 수 있는가
- 어떤 팀/역할이 병목이 되는가
- 어떤 상황에서 약속과 정렬이 무너지는가
- 어떤 pressure에서 persona collapse나 over-accommodation이 생기는가

### non-policy pressure scenario를 추가하는 이유

현재까지의 개선은 policy/social-impact 시나리오 3개에서 확인됐다.

하지만 이 결과만으로는:
- 개선이 엔진 자체의 효과인지
- 아니면 policy negotiation topology에 맞춰진 효과인지
를 완전히 분리할 수 없다.

그래서 다음 검증 축으로 `non-policy pressure scenarios`를 추가한다.

의도:
- policy domain 밖에서도 persona-state stability 개선이 유지되는지 확인
- 특히 launch pressure, trust conflict, integration pressure 같은 다른 pressure topology에서 controller가 여전히 유효한지 확인

### 새 시나리오 track

이번에 두 개의 non-policy scenario를 추가했다.

1. `new_product_launch`
- cross-functional launch pressure
- brand upside vs readiness risk vs operational supportability 충돌

2. `post_merger_integration`
- acquisition 이후 trust / autonomy / retention pressure
- relationship and commitment continuity가 실제로 드러나기 좋은 topology

이 둘은 현재 설계와 잘 맞는다.
이유:
- phase-based tension/negotiation 구조를 그대로 쓸 수 있다
- stakeholder 간 incentive conflict가 분명하다
- 단순 경제 예측이 아니라 interaction failure를 보기 좋다

### 다음 액션

1. 위 두 scenario를 `naive / engine / engine_controller`로 smoke benchmark 실행
2. policy track과 non-policy track의 delta 방향 비교
3. improvement가 유지되면 `runtime robustness evidence`로 기록
4. 특정 track에서만 무너지면 scenario-overfit 가능성으로 해석

## 2026-03-07 15:34 +0800

### non-policy smoke benchmark 결과

새로 추가한 non-policy 시나리오 2개를 각각 `1 script x 1 repetition`으로 smoke benchmark 했다.

실행 시나리오:
- `new_product_launch`
- `post_merger_integration`

산출물:
- `simulation_engine/results_mvp_non_policy_new_product_launch_smoke_2026-03-07-1512`
- `simulation_engine/results_mvp_non_policy_post_merger_integration_smoke_2026-03-07-1523`

### 결과 요약

1. `new_product_launch`에서는 `engine_controller`가 가장 좋았다.
- `naive`: drift `0.1769`, contradiction `0.2857`, envelope `6`
- `engine`: drift `0.1692`, contradiction `0.0000`, envelope `5`
- `engine_controller`: drift `0.1502`, contradiction `0.0000`, envelope `6`

해석:
- controller는 launch/coordination pressure에서는 분명히 유효했다.
- 특히 `C` error가 크게 줄었고 contradiction이 제거됐다.

2. `post_merger_integration`에서는 mixed signal이 나왔다.
- `naive`: drift `0.1651`, contradiction `0.1250`, envelope `7`
- `engine`: drift `0.1527`, contradiction `0.0000`, envelope `6`
- `engine_controller`: drift `0.1565`, contradiction `0.0000`, envelope `8`

해석:
- PMI/trust-heavy topology에서는 `engine`이 headline KPI 기준으로 가장 좋았다.
- controller는 contradiction은 잘 막았지만, `E` error와 envelope 쪽에서 과보정 신호가 있었다.

3. 두 시나리오 평균으로 보면 early signal은 여전히 긍정적이지만, 완전한 robustness pass는 아니다.
- 평균 drift:
  - `naive`: `0.1710`
  - `engine`: `0.1610`
  - `engine_controller`: `0.1534`
- 평균 contradiction:
  - `naive`: `0.2054`
  - `engine`: `0.0000`
  - `engine_controller`: `0.0000`
- 평균 envelope:
  - `naive`: `6.5`
  - `engine`: `5.5`
  - `engine_controller`: `7.0`

즉:
- drift와 contradiction 기준으로는 `engine_controller`가 가장 강하다.
- 하지만 envelope 기준으로는 아직 non-policy pressure에서 불안정성이 남아 있다.

### 의미

현재 엔진은 policy domain에만 묶여 있지는 않다.
최소한 `new_product_launch`에서는 같은 개선 방향이 유지됐다.

하지만 `post_merger_integration` 결과는, 현재 controller가:
- coordination-heavy / execution-heavy topology에는 강하고
- trust / autonomy / relationship-sensitive topology에는 아직 덜 안정적일 수 있음을 시사한다.

즉 현재 결론은:
- `scenario-generalizable early signal`은 있다
- 하지만 `full robustness evidence`라고 말하기에는 아직 이르다
- 특히 PMI류 시나리오에서의 relationship-sensitive control 보강이 필요하다

### 다음 액션

1. `post_merger_integration`를 중심으로 `engine_controller`의 `E` 과상승과 envelope 초과 원인 분석
2. relationship-sensitive phase에서 controller가 구조화/주도성 쪽으로 과보정하지 않도록 조정
3. non-policy track도 `3 repetitions`로 늘려 noise를 줄인 뒤 policy track과 다시 비교

## 2026-03-07 15:49 +0800

### non-policy track 재검증 계획

현재 non-policy 결과는 `1 repetition` smoke라서, `post_merger_integration`의 mixed signal이
- 우연한 run-to-run variance인지
- 실제로 controller가 trust/autonomy topology에서 약한 것인지
를 아직 분리할 수 없다.

따라서 다음 단계는 `new_product_launch`와 `post_merger_integration`를 묶어서
`3 repetitions`로 다시 돌리는 것이다.

### 이번 실행의 목적

1. `PMI` mixed signal이 재현되는지 확인
2. non-policy track에서의 평균 drift / envelope / contradiction 방향 확정
3. policy track 대비 현재 엔진이 어느 topology에서 강하고 약한지 정리

### 이번 실행의 판정 기준

1. `engine_controller`가 non-policy 평균에서 `naive`보다 drift가 낮을 것
2. contradiction은 `0` 또는 `naive`보다 명확히 낮을 것
3. envelope가 계속 `engine`보다 나쁘게 유지되면, 이는 PMI/trust-sensitive topology에서의 구조적 약점으로 기록

## 2026-03-07 17:54 +0800

### non-policy track 3-repetition 결과

두 non-policy 시나리오를 각각 `3 repetitions`로 다시 실행했다.

산출물:
- `simulation_engine/results_mvp_non_policy_new_product_launch_full_2026-03-07-1651`
- `simulation_engine/results_mvp_non_policy_post_merger_integration_full_2026-03-07-1721`

### 결과 요약

1. `new_product_launch`에서는 `engine_controller`가 평균 기준으로 가장 좋았다.
- `naive`: drift `0.1777`, relationship `0.1429`, contradiction `0.2381`, envelope `6.3333`
- `engine`: drift `0.1750`, relationship `0.0708`, contradiction `0.0476`, envelope `7.3333`
- `engine_controller`: drift `0.1738`, relationship `0.0000`, contradiction `0.0000`, envelope `6.3333`

해석:
- launch/coordination topology에서는 controller의 이득이 유지됐다.
- headline drift 이득은 크지 않지만, relationship/contradiction 안정화는 명확하다.

2. `post_merger_integration`에서는 smoke의 mixed signal이 재현되지 않았다.
- `naive`: drift `0.1750`, relationship `0.1000`, contradiction `0.0667`, envelope `6.0000`
- `engine`: drift `0.1823`, relationship `0.0000`, contradiction `0.1528`, envelope `5.6667`
- `engine_controller`: drift `0.1440`, relationship `0.0000`, contradiction `0.0000`, envelope `4.3333`

해석:
- PMI/trust-heavy topology에서도 평균 기준으로는 `engine_controller`가 가장 좋았다.
- 이전 `1 repetition` smoke의 mixed signal은 run-to-run noise일 가능성이 높다.

3. non-policy track 전체 평균도 `engine_controller`가 가장 좋다.
- 평균 drift:
  - `naive`: `0.1764`
  - `engine`: `0.1787`
  - `engine_controller`: `0.1589`
- 평균 relationship inconsistency:
  - `naive`: `0.1215`
  - `engine`: `0.0354`
  - `engine_controller`: `0.0000`
- 평균 contradiction:
  - `naive`: `0.1524`
  - `engine`: `0.1002`
  - `engine_controller`: `0.0000`
- 평균 envelope:
  - `naive`: `6.1667`
  - `engine`: `6.5000`
  - `engine_controller`: `5.3333`

### 의미

이제 current engine에 대해 더 강하게 말할 수 있다.

1. 개선은 policy topology에만 묶인 것이 아니다.
- `launch`
- `PMI`
둘 다에서 `engine_controller`가 평균 기준 최상이다.

2. stakeholder-based pressure engine이라는 framing이 더 타당해졌다.
- 현재 runtime은 policy/social-impact뿐 아니라
- cross-functional launch pressure
- post-merger trust/integration pressure
에서도 persona-state stability를 개선한다.

3. non-policy robustness는 현재 기준으로 pass로 기록한다.
- single-run smoke에서 보였던 PMI mixed signal은 재현되지 않았다.
- 따라서 지금 단계의 해석은 `scenario-overfit`보다 `runtime effect` 쪽이 더 강하다.

### 다음 액션

1. policy track + non-policy track을 합친 전체 robustness summary 정리
2. 그 다음엔 `marketing / company strategy` 계열 시나리오를 더 늘려서 topology coverage 확장
3. 필요하면 그 이후에만 input-understanding 자동화로 넘어감

## 2026-03-07 19:05 +0800

### 목표 전환: dialogue-only -> action-conditioned simulation

현재까지의 benchmark로 `persona-stable dialogue engine`은 충분히 의미 있는 개선을 보였다.
따라서 다음 실험 목표는 단순 대화 안정성에서 `persona-stable action-conditioned simulation`으로 확장한다.

새 핵심 질문은 다음이다.

1. 대화가 typed action으로 더 일관되게 컴파일되는가
2. 그 action이 deterministic world-state transition을 더 안정적으로 만드는가
3. action/state feedback이 다음 phase 대화에 들어가도 persona drift가 다시 커지지 않는가

즉 이제는
- `좋은 대화를 하는가`
보다
- `좋은 대화가 구조화된 행동과 설명 가능한 상태 전이로 이어지는가`
를 본다.

### 왜 목표가 바뀌었는가

이유는 세 가지다.

1. dialogue-only MVP claim은 이미 충분히 검증됐다.
- policy / non-policy 모두에서 `engine_controller`가 `naive`보다 좋았다.

2. end-product 관점에서 다음 병목은 `action/state coherence`다.
- 실제 시뮬레이션은 대화만으로 끝나지 않고
- commitment -> action -> world change -> next-turn feedback
루프를 가져야 한다.

3. 지금 단계에서 graph DB를 붙이는 것보다 작은 typed action layer가 더 높은 ROI를 가진다.
- actor 수와 horizon이 아직 작고
- 필요한 것은 graph retrieval이 아니라 deterministic state transition이다.

### 이번 구현 범위

Action Layer v0는 다음으로 고정한다.

- phase-end apply
- hand-authored deterministic transitions
- all current scripts coverage
- in-memory / JSON ledger 유지
- `naive_action_baseline`, `engine_dialogue_only`, `engine_action_v0`의 3-arm 비교

### 이번 실험의 headline KPI

1. `structured_action_validity_rate`
2. `executed_action_contradiction_rate`
3. `state_transition_coherence`
4. `state_trajectory_variance`
5. `persona_drift_mae_after_action`

보조 KPI:

1. `owner_resolution_rate`
2. `action_feedback_utilization`
3. `relationship_inconsistency_after_action`
4. `commitment_contradiction_after_action`

### 판정 기준

`engine_action_v0`가 `naive_action_baseline`보다 아래를 만족해야 한다.

1. action validity가 높을 것
2. executed action contradiction이 낮을 것
3. state trajectory variance가 낮을 것
4. persona drift가 현재 dialogue-only `engine_controller` 대비 크게 악화되지 않을 것

### 다음 실행 순서

1. smoke
- `commuting_support_policy`
- `new_product_launch`
- 각 1 repetition

2. full
- 현재 5개 script 전체
- 각 3 repetitions

3. stress
- `housing_support_policy`
- `post_merger_integration`
- 각 5 repetitions

### 구현 로그

Action Layer v0 관련 구현 파일:
- `simulation_engine/action_layer.py`
- `simulation_engine/state_store.py`
- `simulation_engine/script.py`
- `simulation_engine/state_ledger.py`
- `simulation_engine/actor.py`
- `simulation_engine/controller.py`
- `simulation_engine/graphs.py`
- `simulation_engine/benchmark.py`
- `simulation_engine/metrics.py`
- `simulation_engine/reporting.py`

관련 문서:
- `2026-03-07-actionlayer-PLAN.md`

## 2026-03-07 19:21 +0800

### dynamic builder layer는 계획만 유지

최종 제품은 user input에서 dynamic stakeholder set과 environment를 자동 생성해야 한다.
하지만 이 builder layer는 지금 바로 구현하지 않고, `dynamicSimulation-PLAN.md`로 설계만 먼저 고정한다.

### 이유

1. 현재 막 검증해야 하는 것은 `Action Layer v0`다.
- runtime/action coherence가 아직 우선 검증 대상이다.

2. builder layer를 지금 같이 구현하면 문제 경계가 무너진다.
- runtime issue
- action issue
- stakeholder generation issue
- environment generation issue
가 섞여서 원인 분리가 어려워진다.

3. 현재 control engine은 이미 generic하다.
- 새 stakeholder마다 별도 controller를 코딩할 필요는 없다.
- 필요한 것은 앞단에서 `StakeholderActorSpec + SimulationScript`를 자동 생성하는 builder다.

### 현재 해석

현재 엔진은:
- `dynamic stakeholder creation engine`은 아직 아니고
- `dynamic script를 받아 실행할 수 있는 runtime kernel`
이다.

따라서 다음 단계의 제품 설계는
- control engine을 새로 만드는 것이 아니라
- `input understanding -> stakeholder expansion -> environment generation -> script builder`
레이어를 추가하는 방향으로 간다.

### 관련 문서

- `dynamicSimulation-PLAN.md`

### immediate next action

1. `Action Layer v0` benchmark 먼저
2. builder layer는 계획 유지
3. runtime/action 결과가 안정되면 그 다음에 builder v0 착수

## 2026-03-07 19:44 +0800

### Action Layer v0 smoke 결과

`commuting_support_policy`, `new_product_launch` 기준 `Action Layer v0` 3-arm smoke를 실행했다.

- 조건:
  - `naive_action_baseline`
  - `engine_dialogue_only`
  - `engine_action_v0`
- repetition:
  - 각 `1`

### 핵심 관찰

1. `engine_action_v0`는 `naive_action_baseline` 대비 분명히 좋아졌다.
- `persona_drift_mae`: `0.1888 -> 0.1540`
- `relationship_inconsistency`: `0.1688 -> 0.0000`
- `commitment_contradiction`: `0.1250 -> 0.0000`
- `structured_action_validity_rate`: `0.2916 -> 0.5000`

2. `engine_dialogue_only` 대비로는 mixed signal이다.
- `persona_drift_mae`는 `0.1423`으로 `engine_action_v0 0.1540`보다 더 좋다.
- 반면 `engine_action_v0`는 envelope가 더 낮고 (`6.0 -> 4.5`), 관계 불안정성도 더 낮다 (`0.0791 -> 0.0000`).

3. v0 action layer는 baseline 대비 유효한 early signal은 보였다.
- 다만 `engine_dialogue_only -> engine_action_v0` 전환 시 drift가 악화되지 않는지를 표본을 늘려 다시 확인해야 한다.

### 현재 판정

- `naive_action_baseline` 대비 claim은 early pass에 가깝다.
- `engine_dialogue_only` 대비 claim은 아직 hold다.
- 따라서 다음 실험은 계획대로 `full 5-script x 3 repetitions`다.

### immediate next action

1. `Action Layer v0 full benchmark`
2. headline KPI:
- `structured_action_validity_rate`
- `executed_action_contradiction_rate`
- `state_transition_coherence`
- `action_feedback_utilization`
- `persona_drift_mae_after_action`
3. full run 결과로
- action layer가 dialogue-only 대비도 유지되는지
- 어느 script / role에서 drift가 다시 올라가는지
를 판정한다.

## 2026-03-07 20:41 +0800

### Action Layer v0 5-script pilot 결과

현재 5개 script 전체를 `1 repetition`으로 돌린 pilot이 완료됐다.

- 조건:
  - `naive_action_baseline`
  - `engine_dialogue_only`
  - `engine_action_v0`
- 범위:
  - policy 3개
  - non-policy 2개

### 요약

1. `engine_action_v0`는 `naive_action_baseline` 대비로는 유효하다.
- `persona_drift_mae`: `0.1851 -> 0.1676`
- `envelope_violations`: `6.8 -> 5.2`
- `commitment_contradiction`: `0.0857 -> 0.0`

2. 하지만 `engine_dialogue_only` 대비로는 아직 partial fail이다.
- `persona_drift_mae`: `0.1568 -> 0.1676`로 악화
- `structured_action_validity_rate`: `0.2933 -> 0.4739`로 개선되지만
- 계획 문서의 pass 기준인 `>= 0.85`와는 거리가 멀다
- `state_trajectory_variance`도 `0.0 -> 0.0018`로 증가했다

3. 시나리오별 편차가 크다.
- 상대적으로 좋은 쪽:
  - `commuting_support_policy`
- mixed 또는 약한 쪽:
  - `youth_employment_policy`
  - `new_product_launch`
  - `post_merger_integration`

### 현재 해석

Action Layer v0는 `baseline 대비 더 구조화된 action-conditioned run`이라는 초기 신호는 있다.
하지만 아직 `dialogue-only engine 위에 action layer를 얹어도 persona stability가 유지된다`는 강한 claim은 못 한다.

현재 가장 가능성이 큰 원인은:
- action compiler의 parse 성공률이 불안정함
- action-aware scoring이 action validity를 충분히 끌어올리지 못함
- 일부 script에서 action feedback이 persona drift를 다시 키움

### immediate next action

1. 이미 시작된 `3 repetitions full run`은 끝까지 유지한다
2. full 결과로 pilot mixed signal이 재현되는지 확인한다
3. 재현되면 바로 `v0.1` 수정에 들어간다
- action compiler robustness
- action-aware scoring weight
- script별 action ontology / target_key 설계

## 2026-03-07 21:54 +0800

### Action Layer v0 full 결과

`5 scripts x 3 conditions x 3 repetitions` full run이 완료됐다.

### 전체 판정

1. `engine_action_v0`는 `naive_action_baseline` 대비로는 pass다.
- `persona_drift_mae`: `0.1793 -> 0.1676`
- `commitment_contradiction`: `0.2897 -> 0.0`
- `owner_resolution_rate`: `0.8667 -> 1.0`
- `state_transition_coherence`: `0.8667 -> 1.0`
- `action_feedback_utilization`: `0.1000 -> 0.5555`

2. 하지만 `engine_dialogue_only` 대비로는 partial fail이다.
- `persona_drift_mae`: `0.1692 -> 0.1676`로 소폭 개선
- `C` error는 좋아짐: `0.2642 -> 0.2478`
- 그러나 `structured_action_validity_rate`: `0.5894 -> 0.5282`로 악화
- `relationship_inconsistency`: `0.0 -> 0.0961`로 악화
- `executed_action_contradiction_rate`: `0.0 -> 0.0667`로 악화
- `state_trajectory_variance`: `0.0008 -> 0.0014`로 악화

3. 따라서 v0의 현재 해석은 다음과 같다.
- dialogue-only보다 `더 actionable`하다고 말하기는 아직 이르다
- baseline보다 `더 consistent한 action-conditioned run`이라고는 말할 수 있다
- 즉 v0는 `제품 실험용 prototype pass`, `strong product claim fail` 상태다

### 시나리오 해석

상대적으로 잘 맞는 시나리오:
- `commuting_support_policy`
- `post_merger_integration`

약한 시나리오:
- `housing_support_policy`
- `new_product_launch`
- `youth_employment_policy`

특히 `housing_support_policy`는 drift `0.1940`, relationship inconsistency `0.1833`으로 hardest case였다.

### role-level 해석

Action Layer v0에서 hardest role:
- `Small property owner`
- `District housing administrator`
- `Adjacent local merchant`
- `People and integration lead`
- `Product launch lead`
- `Operations and reliability lead`

공통 패턴:
- 방어적 이해관계가 강함
- low-openness 또는 high-constraint 성격을 유지해야 함
- action feedback이 들어오면 더 주도적/구조적으로 drift하기 쉬움

### immediate next action

1. `Action Layer v0.1` 수정
2. 우선순위:
- action proposal / rejection audit를 benchmark output에 저장
- action compiler robustness 강화
- action-aware scoring을 validity 중심으로 더 강하게 연결
- script별 `allowed_action_types`와 `target_key`를 더 좁게 설계
3. 수정 후
- `smoke -> pilot` 재실행
- full run은 v0.1에서 다시 돌린다

## 2026-03-07 22:18 +0800

### Action Layer v0.1 smoke 판정

`commuting_support_policy`, `new_product_launch` 기준 v0.1 smoke를 실행했다.

### 핵심 결과

- `structured_action_validity_rate`는 모든 조건에서 `1.0`이 됐다.
- 하지만 `engine_action_v0`의 persona stability는 오히려 나빠졌다.

전체 smoke 평균:
- `naive_action_baseline`: drift `0.1625`
- `engine_dialogue_only`: drift `0.1483`
- `engine_action_v0`: drift `0.1817`

script별:
- `commuting_support_policy`
  - `0.1614 -> 0.1246 -> 0.1701`
- `new_product_launch`
  - `0.1636 -> 0.1720 -> 0.1934`

### 현재 해석

v0.1은 `syntactic validity`는 사실상 해결했다.
하지만 지금 bottleneck은 더 이상 JSON/validator가 아니라, **semantic action quality와 persona fit**이다.

즉 현재 문제는:
- action이 너무 쉽게 valid가 된다
- 하지만 그 action이 actor/phase/world-state에 truly aligned된 것은 아니다

### 결정

1. `Method 1` 추가 강화는 지금 보류
- 더 강한 schema/constrained generation을 지금 더 넣을 필요는 없다
- syntactic validity는 이미 ceiling에 닿았다

2. `Method 2`는 바로 진행
- authoritative action planning을 planner artifact로 승격
- `Say -> Extract`보다 `Decide -> Say`로 바꾼다

3. `Phase 0 observability`도 바로 진행
- planner action
- selected action hint
- compiled proposal
- arbitration 결과
- executed action
를 모두 audit chain으로 남긴다

### immediate next action

1. `Action Layer v0.2`
2. scope:
- `Phase 0 observability`
- `Method 2 (action-first)`
3. `Method 1`의 추가 constrained generation은 v0.2 이후 필요할 때만 재검토

## 2026-03-07 22:46 +0800

### Action Layer v0.2 착수

`v0.2`는 두 가지에만 집중한다.

1. `Phase 0 observability`
2. `Method 2 (action-first)`

### 왜 이 방향인가

`v0.1` smoke에서 `structured_action_validity_rate`는 ceiling에 도달했지만, `engine_action_v0`의 drift는 오히려 악화됐다.

이건 병목이 더 이상 syntax가 아니라는 뜻이다.
현재 병목은:
- planner intent와 compiled action 사이의 semantic mismatch
- 그 mismatch가 어디서 생기는지 추적할 audit chain 부재

### 이번 변경 목표

- planner가 만든 `planned_action_artifact`를 authoritative source로 승격
- compile path를 `planned_action -> hint -> heuristic -> llm` 순으로 재구성
- per-turn action audit chain을 결과 JSON에 남김
- `action_plan_alignment_mean`, `planned_action_coverage_rate`를 새 진단 metric으로 추가

### immediate next action

1. `v0.2 smoke`
2. scripts:
- `commuting_support_policy`
- `new_product_launch`
3. focus:
- `engine_action_v0`가 `engine_dialogue_only` 대비 덜 무너지는지
- audit chain이 semantic failure를 바로 설명해주는지

## 2026-03-08 00:19 +0800

### v0.2 smoke 후속 판단

`v0.2` smoke는 observability와 planner-action alignment 자체는 성공했다.
하지만 `engine_action_v0`는 여전히 `engine_dialogue_only`보다 drift와 envelope가 나빴다.

핵심 해석:
- syntax 문제는 아님
- alignment 문제도 아님
- 남은 병목은 `role flattening`이다

즉 phase별 action convergence가 강해지면서 서로 다른 역할이 비슷한 action family로 몰리고 있다.

### research-guided next step

논문과 현재 smoke 결과를 합쳐서 다음 수정 방향을 고정한다.

1. `role-conditioned action priors`
- 역할, incentive, concern, phase cue를 보고 preferred action family와 target key를 만든다

2. `conditional action pressure`
- planner-produced action artifact가 없을 때는 action-aware scoring pressure를 크게 낮춘다

3. `observability 유지`
- `v0.2`의 audit chain은 그대로 유지한다

### why

- Role fidelity는 말투뿐 아니라 role capability boundary를 지켜야 한다
- multi-turn discourse는 persona drift를 쉽게 만든다
- reasoning/acting coupling은 필요하지만 모든 turn에서 동일 강도로 강제되면 flattening이 생긴다

### immediate next action

1. `v0.3` 수정
2. same smoke rerun
- `commuting_support_policy`
- `new_product_launch`
3. 판단 기준
- `engine_action_v0` drift가 `v0.2`보다 내려가는지
- action validity / alignment는 유지되는지

## 2026-03-08 00:54 +0800

### v0.3 smoke 결과

`role-conditioned action priors + conditional action pressure`를 넣은 `v0.3` smoke를 실행했다.

### 핵심 결과

전체 평균:
- `naive_action_baseline`: drift `0.1684`, envelope `7.0`
- `engine_dialogue_only`: drift `0.1682`, envelope `7.5`
- `engine_action_v0`: drift `0.1731`, envelope `6.5`

### 해석

1. `v0.3`는 `v0.2`보다 부분 개선이다
- `engine_action_v0` drift: `0.1738 -> 0.1731`
- `engine_action_v0` envelope: `7.0 -> 6.5`
- `planned_action_coverage_rate`: `0.50 -> 1.00`

2. 하지만 아직 `engine_dialogue_only`를 이기지 못했다
- drift는 여전히 더 나쁨
- 특히 `new_product_launch`에서 `engine_action_v0`가 `0.2026`으로 가장 나쁨

3. role prior는 실제로 action diversity를 늘렸다
- `commuting_support_policy`와 `new_product_launch` 둘 다 `v0.2`보다 role별 action family 분화가 늘었다
- 하지만 `NEGOTIATION` phase에서는 여전히 phase-level convergence가 남아 있다

### 현재 결론

- `Method 2` 자체는 맞다
- `role prior`도 필요한 방향이다
- 남은 병목은 `phase-level convergence`, 특히 `NEGOTIATION`에서 여러 actor가 같은 action family로 몰리는 현상이다

### immediate next action

1. `v0.4`
2. 목표:
- `NEGOTIATION`에서 role-diversity guard 추가
- action family convergence penalty 추가
- strongest pressure phase에서 actor별 action family가 더 분리되도록 조정

## 2026-03-08 01:32 +0800

### v0.4 구현 시작

`Action Layer v0.4`는 더 강한 parser나 더 많은 LLM을 붙이는 단계가 아니다.  
현재 병목은 syntax가 아니라 `NEGOTIATION` phase의 same-family convergence다.

### 구현 목표

1. `SimulationScript.metadata`에 phase/actor action preference를 올린다
2. controller가 현재 phase에서 이미 사용된 action family를 직접 패널티로 본다
3. `engine_action_v0` 평가 KPI를 `validity` 중심에서 `diversity/convergence` 중심으로 확장한다

### 새 headline KPI

- `action_family_convergence_rate`
- `role_action_diversity_score`
- `negotiation_uniqueness_rate`

### 판단 기준

- `engine_action_v0`는 이제 `naive_action_baseline`뿐 아니라 `engine_dialogue_only`를 직접 baseline으로 비교한다
- 특히 `new_product_launch`에서 drift와 convergence가 같이 개선되어야 한다

## 2026-03-08 03:36 +0800

### 운영 전략 수정

`v0.4` full run은 45-session monolithic 실행으로 시작했지만, 결과 디렉터리 없이 너무 오래 정지 상태가 이어졌다.  
실험 자체를 바꾸는 것이 아니라 운영 방식을 바꿨다.

### 변경 사항

1. benchmark harness가 이제 각 run 완료 시점마다 partial outputs를 저장한다
2. output dir 안에 `benchmark_runs.json`, `benchmark_aggregate.json`, `benchmark_report.md`를 계속 갱신한다
3. 추가로 `progress.json`에 `completed_runs`, `total_runs`, 마지막 완료 script/condition을 남긴다

### 이유

- 긴 action-layer benchmark는 호출 수가 많아 단일 대형 런이 중간 실패하면 전부 잃기 쉽다
- partial checkpoint가 있어야 10분 단위 모니터링과 자동 다음 실험 연결이 가능하다
- 이 변경은 scoring, controller, runtime logic을 바꾸지 않고 observability/운영 안정성만 높인다

### immediate next action

1. `v0.4` full run 재시작
2. 완료 시 자동 분석
3. 통과하면 hardest non-policy 2-script stress pack으로 자동 이동

## 2026-03-08 03:46 +0800

### 운영 최적화 추가

`v0.4` full run checkpoint는 정상적으로 찍혔지만, run당 latency가 너무 길어 전체 실험이 비현실적으로 늘어지는 문제가 확인됐다.

### 변경 사항

1. `ExperimentCandidateAgent.generate_candidate_pool`
2. `ExperimentCandidateAgent.generate_candidate_pool_styles`

위 두 경로의 candidate generation을 직렬 호출에서 `asyncio.gather(...)` 기반 병렬 호출로 변경했다.

### 이유

- action-layer benchmark는 planner 1회 + candidate 4회 + action compile 1회가 기본이라 candidate generation이 가장 큰 병목이다
- 이 변경은 scoring, controller, ontology, KPI를 바꾸지 않고 call scheduling만 바꾼다
- 따라서 실험 의미는 유지하면서 full/stress 실행 시간을 줄이는 운영 최적화로 본다

### immediate next action

1. 병렬화 반영된 `v0.4` full run 재시작
2. checkpoint 기반으로 진행률 모니터링
3. 결과 판정 후 stress pack 자동 실행 여부 결정

## 2026-03-08 07:58 +0800

### v0.4 full 판정

`v0.4` full run은 monolithic 45-session을 checkpoint 저장형으로 재실행했고, `post_merger_integration` 구간은 장시간 정체되어 별도 rerun으로 분리했다.  
하지만 이미 완료된 4개 script 기준으로 `engine_action_v0`가 `engine_dialogue_only`보다 평균 drift에서 크게 뒤져, full pass는 수학적으로 뒤집히지 않는 상태가 확인됐다.

### 현재 결론

- `v0.4`는 fail
- `role-diversity guard + duplication penalty`만으로는 부족하다
- 핵심 병목은 `same-family convergence` 그 자체보다 `모든 actor를 action-bearing으로 밀어버리는 action pressure`다

### evidence

- completed 4-script aggregate drift
  - `engine_dialogue_only`: `0.1543`
  - `engine_action_v0`: `0.1737`
- hardest case `new_product_launch`
  - `engine_dialogue_only`: `0.1538`
  - `engine_action_v0`: `0.1695`

## 2026-03-08 08:03 +0800

### v0.5 구현 시작

다음 가설은 `action sparsity + family cap`이다.

### 구현 목표

1. 모든 actor가 모든 phase에서 action-bearing일 필요가 없도록 `allow_no_action`을 정책화한다
2. phase별 `family_cap`, `max_actions_per_phase`, `sparsity_threshold`를 둔다
3. planner action이 있어도 actor-local need가 약하면 `sparse_action_gate`로 proposal을 비활성화한다
4. controller도 같은 기준으로 `action_sparsity_penalty`를 반영한다

### immediate next action

1. `v0.5` smoke
2. scripts:
- `commuting_support_policy`
- `new_product_launch`
3. conditions:
- `naive_action_baseline`
- `engine_dialogue_only`
- `engine_action_v0`

## 2026-03-08 09:05 +0800

### v0.5 smoke 결과

`v0.5`는 fail이다.

### 핵심 결과

- aggregate drift
  - `naive_action_baseline`: `0.1531`
  - `engine_dialogue_only`: `0.1515`
  - `engine_action_v0`: `0.1672`
- hardest case `new_product_launch`
  - `engine_dialogue_only`: `0.1649`
  - `engine_action_v0`: `0.1817`
- success case `commuting_support_policy`
  - `engine_dialogue_only`: `0.1382`
  - `engine_action_v0`: `0.1527`

### 해석

- `sparse_action_gate`는 들어갔지만 실제 smoke에서
  - `planned_action_coverage_rate = 1.0`
  - `action_family_convergence_rate = 0.75`
  - `role_action_diversity_score = 0.5208`
  로 그대로였다
- 즉 action pressure를 실제로 줄이지 못했고, 결과적으로 `engine_action_v0`가 `engine_dialogue_only`를 다시 못 이겼다

### 다음 가설

다음 수정은 `late-phase / selective action activation`이다.

### immediate next action

1. `OPENING/TENSION`은 action을 shadow planning만 하고 실행/feedback을 늦춘다
2. `NEGOTIATION/CLOSING` 중심으로만 action-aware scoring과 transition을 활성화한다
3. `v0.6` smoke로 다시 `commuting_support_policy + new_product_launch`를 검증한다

## 2026-03-08 09:16 +0800

### v0.6 구현 시작

이번 가설은 `late-phase / selective action activation`이다.

### 구현 포인트

1. `phase_action_policy.action_mode`
- `OPENING`, `TENSION`: `shadow`
- `NEGOTIATION`, `CLOSING`: `execute`
2. `shadow` phase에서는 action compile/audit는 유지하되
- controller의 action-aware scoring 비활성화
- proposal ledger append 비활성화
- arbitration / transition / phase feedback 실행 안 함

### 기대 효과

- early phase 대화 품질을 `engine_dialogue_only`에 더 가깝게 유지
- late phase에서만 action-conditioned world update를 수행
- hardest-case `new_product_launch`에서 drift를 줄일 것

## 2026-03-08 09:24 +0800

### v0.6 smoke 결과

`v0.6`도 fail이다.

### 핵심 결과

- aggregate drift
  - `naive_action_baseline`: `0.1649`
  - `engine_dialogue_only`: `0.1447`
  - `engine_action_v0`: `0.1741`
- `new_product_launch`
  - `engine_dialogue_only`: `0.1720`
  - `engine_action_v0`: `0.1719`
  - hardest-case에서는 거의 같아졌지만
- `commuting_support_policy`
  - `engine_dialogue_only`: `0.1174`
  - `engine_action_v0`: `0.1764`
  - success-case를 심하게 망쳤다

### 해석

- `late-phase gating`은 hardest-case를 약간 완화했지만
- action-aware scoring이 success-case를 계속 손상시킨다
- 따라서 현재 evidence 기준으로는 `engine_action_v0`를 더 밀지 않고, `engine_dialogue_only`를 action-conditioned runtime의 production candidate로 보는 게 맞다

### 다음 실험 목표

`engine_dialogue_only`를 baseline이 아니라 현재 best action runtime candidate로 승격해서 full 5-script validation을 한다.

### immediate next action

1. `naive_action_baseline` vs `engine_dialogue_only`
2. current 5 scripts
3. 3 repetitions
4. 이 2-arm full run에서 product candidate 판정

## 2026-03-08 09:36 +0800

### `engine_action_v0` vs `engine_dialogue_only` 차이 해석

둘 다 `action layer`는 켜져 있다. 차이는 `action`이 대화 선택을 직접 오염시키느냐이다.

- `engine_dialogue_only`
  - `engine_controller` 기반
  - `use_action_layer=True`
  - `use_action_aware_scoring=False`
  - 즉 대화 후보는 기존 personality-fidelity controller로 고르고, 선택된 발화 뒤에 action proposal을 붙인다
- `engine_action_v0`
  - `engine_controller` 기반
  - `use_action_layer=True`
  - `use_action_aware_scoring=True`
  - 즉 후보 선택 단계에서부터 `action_executability`, `state_consistency`, `action_plan_alignment`, `action_role_fit` 같은 action-aware term이 점수에 들어간다

### 현재 결론

personality fidelity controller 자체는 `engine_dialogue_only` 쪽이 더 잘 유지된다.  
문제는 `action layer` 자체가 아니라, `action desirability`를 후보 selection objective 안에 넣는 방식이다.

### 왜 `engine_action_v0`가 더 나빠졌는가

1. action-aware scoring은 action으로 잘 번역되는 발화를 더 선호한다
2. 그런 발화는 대체로 더 구조적이고, 더 overtly initiative-taking하며, 더 비슷한 phase action family로 수렴한다
3. 그 결과 actor 간 role differentiation이 줄고 `E`, 일부 `C`가 과상승한다
4. 즉 `action-conditioned simulation`은 되지만, persona-stable dialogue selection을 오염시킨다

### 반복된 증거

- `v0.5` aggregate drift
  - `engine_dialogue_only`: `0.1515`
  - `engine_action_v0`: `0.1672`
- `v0.6` aggregate drift
  - `engine_dialogue_only`: `0.1447`
  - `engine_action_v0`: `0.1741`
- `v0.6` success-case `commuting_support_policy`
  - `engine_dialogue_only`: `0.1174`
  - `engine_action_v0`: `0.1764`

### 현재 product 해석

현재 best candidate는 `engine_action_v0`가 아니라 `engine_dialogue_only`다.  
즉 현재 제품 후보는 `dialogue-first, action-second` 구조이고, `action-first scoring`은 아직 production candidate가 아니다.

## 2026-03-08 10:11 +0800

### `engine_dialogue_only` 5-script full validation 완료

정책 3개와 non-policy 2개를 합쳐 `naive_action_baseline` vs `engine_dialogue_only`를 최종 비교했다.  
이전 `30-run full`은 중간 종료되어 policy 3개만 남았고, non-policy 2개는 별도 rerun으로 보완해 전체 5-script 결과를 합산했다.

### 전체 판정

`engine_dialogue_only`를 현재 action-conditioned runtime의 production candidate로 채택한다.

### 전체 5-script 평균

- `naive_action_baseline`
  - drift `0.1873`
  - relationship inconsistency `0.0863`
  - commitment contradiction `0.0695`
  - envelope `6.9334`
- `engine_dialogue_only`
  - drift `0.1587`
  - relationship inconsistency `0.0300`
  - commitment contradiction `0.0111`
  - envelope `5.4000`

### 해석

1. personality fidelity와 interaction consistency는 `engine_dialogue_only`가 안정적으로 우세하다
2. action validity/coherence도 `engine_dialogue_only`가 충분히 높다
3. 다만 `action_family_convergence=0.8000`, `role_action_diversity=0.4916`, `negotiation_uniqueness=0.4000`으로 action 다양성은 낮다
4. 즉 현재 candidate는 `안정적 guided runtime`에는 맞지만, `exploratory open-ended runtime`에는 그대로 쓰기 어렵다

### script별 해석

- policy 3개에서는 `engine_dialogue_only`가 명확히 우세
- non-policy 2개 중 `post_merger_integration`는 우세
- `new_product_launch`는 drift `0.1854`로 `naive 0.1848`과 거의 같다
- 즉 hardest case는 여전히 `launch_pressure` family다

### 다음 목표

다음 단계는 `single guided runtime`을 더 억지로 일반화하는 것이 아니라,

- `guided / outcome-anchored` track
- `exploratory / open-ended` track

의 two-track simulation UX를 설계하고, 시나리오 family별 KPI를 분리하는 것이다.

### immediate next action

1. `SimulationScript`에 `simulation_mode`, `scenario_family`, `outcome_spec`를 올린다
2. current guided scripts와 exploratory pressure scripts를 같은 contract로 다룬다
3. benchmark/reporting도 mode/family 단위 집계를 추가한다
4. 다음 실험은 `guided`와 `exploratory`를 섞어 평균내지 않고, 트랙별로 separate KPI를 본다

## 2026-03-08 10:18 +0800

### two-track simulation 구현 시작

다음 제품 단계는 `guided`와 `exploratory`를 같은 평균으로 섞지 않는 것이다.

### 반영한 것

1. `SimulationScript`
- `simulation_mode`
- `scenario_family`
- `outcome_spec`
를 contract에 추가

2. runtime summary / benchmark / reporting
- mode/family 단위 집계를 추가

3. exploratory pressure scripts 추가
- `brand_crisis_response`
- `resource_reallocation_crunch`

### immediate next action

1. exploratory scripts에서 `naive_action_baseline` vs `engine_dialogue_only` smoke
2. exploratory KPI baseline 확보
3. 결과를 바탕으로
- `guided`는 stability-first
- `exploratory`는 diversity-aware
policy를 분리할지 판단
